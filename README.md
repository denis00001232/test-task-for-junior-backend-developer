# Task Service

Сервис для управления задачами с HTTP API на Go.

## правки

### Периодичность задач

**Почему я решил построить БД именно так:**

Во-первых, я выбрал гибкую схему хранения периодичности, которая позволяет поддерживать разные типы повторения задач в одной таблице. Вместо создания отдельных таблиц для каждого типа периодичности, я добавил несколько опциональных полей в основную таблицу `tasks`:

- `periodicity_type` - определяет тип периодичности (daily interval, monthly days, specific dates, odd even)
- `daily_interval` - для повторения каждые N дней
- `monthly_days` - массив дней месяца [1, 15, 30]
- `specific_dates` - массив конкретных дат
- `odd_even_type` - для повторения по четным/нечетным дням

**Преимущества подхода:**

1. **Единая таблица** - все данные задачи в одном месте, нет необходимости в JOIN'ах (На самом деле это компромис, если система разрастется - без нормальных отдельных табличек и джоинов не получится)
2. **Гибкость** - легко добавить новые типы периодичности без изменения схемы
3. **Производительность** - простые запросы без дополнительных связей
4. **Валидация на уровне приложения** - сложные правила проверки реализованы в Go коде, а не в constraints (Почему gpt посчитал эти явным преимуществом - сложно сказать, по идее и там и там делать валидацию было бы идеально)

**Правки:**

- Добавлены поля периодичности во все слои архитектуры (domain, usecase, repository, transport)
- Реализована валидация для каждого типа периодичности
- Обновлена OpenAPI документация с новыми полями
- Настроено правильное сканирование полей из PostgreSQL с помощью pgx

## Требования

- Go `1.23+`
- Docker и Docker Compose

## Быстрый запуск через Docker Compose

```bash
docker compose up --build
```

После запуска сервис будет доступен по адресу `http://localhost:8080`.

Если `postgres` уже запускался ранее со старой схемой, пересоздай volume:

```bash
docker compose down -v
docker compose up --build
```

Причина в том, что SQL-файл из `migrations/0001_create_tasks.up.sql` монтируется в `docker-entrypoint-initdb.d` и применяется только при инициализации пустого data volume.

## Swagger

Swagger UI:

```text
http://localhost:8080/swagger/
```

OpenAPI JSON:

```text
http://localhost:8080/swagger/openapi.json
```

## API

Базовый префикс API:

```text
/api/v1
```

Основные маршруты:

- `POST /api/v1/tasks` - создание задачи с периодичностью
- `GET /api/v1/tasks` - список всех задач
- `GET /api/v1/tasks/{id}` - получение задачи по ID
- `PUT /api/v1/tasks/{id}` - обновление задачи (включая периодичность)
- `DELETE /api/v1/tasks/{id}` - удаление задачи

## Примеры запросов с периодичностью

### Создание задачи с ежедневным повторением:
```json
POST /api/v1/tasks
{
  "title": "Ежедневный отчет",
  "description": "Отправлять отчет каждый 3-й день",
  "status": "new",
  "periodicity_type": "daily interval",
  "daily_interval": 3
}
```

### Создание задачи с ежемесячным повторением:
```json
POST /api/v1/tasks
{
  "title": "Ежемесячная проверка",
  "description": "Проверка 1-го и 15-го числа каждого месяца",
  "status": "new",
  "periodicity_type": "monthly days",
  "monthly_days": [1, 15]
}
```

### Создание задачи на конкретные даты:
```json
POST /api/v1/tasks
{
  "title": "Праздничные задачи",
  "description": "Задачи на конкретные даты",
  "status": "new",
  "periodicity_type": "specific dates",
  "specific_dates": ["2026-12-31T00:00:00Z", "2027-01-01T00:00:00Z"]
}
```

### Создание задачи по четным/нечетным дням:
```json
POST /api/v1/tasks
{
  "title": "Четные дни",
  "description": "Выполняется только по четным дням",
  "status": "new",
  "periodicity_type": "odd even",
  "odd_even_type": "even"
}
```
